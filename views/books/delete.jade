extends ../layout.jade
block body
    .container
        .card
            h1 Eliminar -  #{book.title}
            p ¿Seguro que quires eliminar este libro?
            .actions
                form(action="/book/#{book.id}?_method=DELETE" method="POST")
                    input(type="submit" class="btn btn-danger" value="Confirmar")
